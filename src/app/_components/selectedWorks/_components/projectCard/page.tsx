import styles from './styles.module.css'

// TODO: make project card component (use the one from projects page)

export default function ProjectCard() {
    return (
        <div className={styles.project__container}>
            <p>Project Card</p>
        </div>
    )
}